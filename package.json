{
  "name": "compile-mime-match",
  "version": "0.1.0",
  "description": "Compiles a function that matches a given MIME type.",
  "main": "compile-mime-match.js",
  "files": [
    "compile-mime-match.js"
  ],
  "engines": {
    "node": ">=6"
  },
  "license": "MIT",
  "author": "Nathan Woltman <nwoltman@outlook.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/medleyjs/compile-mime-match.git"
  },
  "homepage": "https://github.com/medleyjs/compile-mime-match#readme",
  "bugs": {
    "url": "https://github.com/medleyjs/compile-mime-match/issues"
  },
  "keywords": [
    "compile",
    "mime",
    "content",
    "type",
    "match",
    "medley",
    "fast"
  ],
  "dependencies": {},
  "devDependencies": {
    "coveralls": "^3.0.4",
    "eslint": "^5.16.0",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1"
  },
  "scripts": {
    "lint": "eslint *.js test/*.js",
    "test-only": "nyc --reporter=html --reporter=text-summary mocha",
    "test": "npm run lint && npm run test-only",
    "coveralls": "nyc report --reporter=text-lcov | coveralls"
  }
}
